# Design

验证路径采用 API 与浏览器双轨：

- API：用临时管理员登录后请求 Dashboard 的真实后端端点，确认数据结构可被前端消费，空数据也返回稳定结构。
- 浏览器：通过首页登录表单进入 `/dashboard`，检查页面能真实渲染统计卡片、图表区、对话表格和空状态。
- 截图：复用 in-app Browser 的 viewport 能力，保存桌面和 390px 移动端截图到任务目录。
- 清理：验证结束后标记临时管理员删除，避免留下可登录账号。
