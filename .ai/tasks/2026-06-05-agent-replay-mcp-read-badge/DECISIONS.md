# Decisions

- Reuse the existing `REPLAY EXPORT` panel instead of creating a new rail card.
- Show the planned MCP call even before backend save, but label it `Save required`.
- Keep copy fallback visible instead of silently failing in headless or restricted browser contexts.
