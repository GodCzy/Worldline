# Decisions

## 2026-06-05

- Use the current Event Detail state instead of creating a separate selected replay state.
- Keep replay lane inside the right rail to preserve the existing Agent workbench layout.
- Use derived diff chips first; richer backend event deltas can later replace or enrich the same chip contract.
