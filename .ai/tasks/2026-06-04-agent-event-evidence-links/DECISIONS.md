# Agent Event Evidence Links Decisions

更新时间：2026-06-04

## D1. 只扩展 summary，不改事件顶层 schema

run ledger 仍处在 Stage 2 文件账本阶段。把链接线索放入 summary 能保持后端契约兼容，同时让前端先获得可审查产品形态。

## D2. 前端 chips 只做可见线索，不做跨页跳转

当前目标是增强 Agent 工作台回放能力。跨 EvidenceAnchor / MCP audit / quality gate 的深跳转需要更多路由和数据契约，留到下一阶段。
