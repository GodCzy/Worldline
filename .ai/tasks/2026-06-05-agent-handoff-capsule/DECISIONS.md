# Decisions

- Keep the handoff capsule inside Replay Export instead of adding another rail panel.
- Use read-only MCP metadata only; no new write path is introduced.
- Treat the capsule as a receiving-Agent contract, not as an automatic executor.
