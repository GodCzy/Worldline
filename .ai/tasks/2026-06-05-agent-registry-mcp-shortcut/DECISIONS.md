# Decisions

- Keep the global MCP Readable panel for the primary replay export.
- Add item-level shortcuts for Registry artifacts so Handoff capsules have an equally explicit read path.
- Use planned logical URIs in local preview instead of hiding the shortcut until backend save.
