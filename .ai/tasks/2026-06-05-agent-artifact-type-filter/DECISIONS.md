# Decisions

- Show planned local artifacts so the feature remains useful while backend/admin save is unavailable.
- Keep saved artifacts authoritative when IDs collide with planned artifacts.
- Use existing focus behavior instead of adding a separate artifact details modal.
