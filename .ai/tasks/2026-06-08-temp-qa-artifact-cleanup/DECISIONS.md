# 决策记录

## 只删除可再生浏览器临时产物

原因：当前 `.ai/tasks` 中的 profile 和 CDP 日志只用于浏览器 QA 运行期，不是长期证据。截图、报告、脚本和 Markdown 任务证据才是可复盘资料。

## 二次扫描后继续清理 2026-06-07 残留 profile

原因：第一批清理来自 Agent/run 目录审计，但 P2-3 的目标是临时 QA 产物清理。复扫发现 3 个同类 `chrome-profile-cdp`，均位于 `.ai/tasks` 内且属于可再生浏览器 profile，因此一并清理，避免留下同类残留。

## 不处理提交

原因：P2-4 才是工作树分组与提交边界。本任务只负责减少临时产物噪音，不对 git stage 或 commit 做操作。
