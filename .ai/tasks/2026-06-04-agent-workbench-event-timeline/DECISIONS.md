# Agent Workbench Event Timeline Decisions

更新时间：2026-06-04

## D1. 真实事件优先，本地预览兜底

后端暂不可用仍是当前产品要求之一。事件时间线不能因为未登录或无管理员凭据而空白，因此本地预览事件用于展示结构，真实后端事件到达后替换预览。

## D2. 不改后端事件 schema

Stage 2 run ledger 已提供足够字段，前端只做消费和格式化，避免扩大后端变更面。
