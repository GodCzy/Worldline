# Decisions

- Reuse the Registry MCP shortcut helper to avoid divergent payload formats.
- Keep the artifact focus button separate from the MCP copy button to avoid nested button markup.
- Use the existing `artifactRegistryMessage` area for copy/fallback status.
