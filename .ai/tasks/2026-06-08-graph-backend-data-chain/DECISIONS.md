# Decisions

日期：2026-06-08

## D1. 先修统一图谱路由的降级边界

原因：真实服务当前没有知识库数据，无法复现旧交接中的具体 `/graph` 500；但代码审计发现 `_get_graph_adapter()` 的实现与注释不一致，可能导致 LightRAG 图谱受 Upload/Neo4j `graph_base` 状态影响。这是图谱页稳定性的直接风险。

## D2. 继续使用临时 superadmin 做本地 QA

原因：图谱和知识库接口需要管理员权限。临时账号只用于本地 QA，随机密码不写入仓库或总结，完成后重新标记 `is_deleted=1`。
