# Decisions

- Use the existing controlled `worldline` MCP boundary rather than adding a new MCP server.
- Keep the tool read-only and service-backed.
- Audit read inspection summaries to make external Agent reads visible without giving direct ledger-file access.
