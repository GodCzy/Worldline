# Decisions

## 2026-06-05

- Keep export local-first so it works while backend is unavailable.
- Export both machine-readable JSON and human-readable Markdown.
- Include focused Dossier and selected event because those are the reviewer context at export time.
