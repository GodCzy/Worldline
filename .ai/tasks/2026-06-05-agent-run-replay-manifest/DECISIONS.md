# Decisions

## 2026-06-05

- Use the existing Focus Dossier system for run replay rather than adding a separate manifest panel.
- Add optional local `episodeIds` and `skillProposalIds` arrays for clickable preview links while keeping backend-provided count fields visible.
- Branch focus selects the active worldline branch, preserving canvas and inspector synchronization.
