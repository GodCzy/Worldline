# Decisions

- Reuse the existing artifact registry endpoint instead of introducing a dedicated handoff endpoint.
- Keep saved handoff capsules as artifacts, not as executable jobs.
- Preserve current admin gating and local-preview fallback behavior.
